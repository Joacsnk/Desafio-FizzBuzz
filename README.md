# 🐍 Desafio FizzBuzz em Python

Este repositório apresenta uma solução elegante e animada para o clássico desafio de lógica **FizzBuzz**, desenvolvida em Python.

## 📝 O Desafio
O objetivo é percorrer números de 1 a 100 e aplicar as seguintes regras:
* Se o número for múltiplo de **3**, exiba `"Fizz"`.
* Se o número for múltiplo de **5**, exiba `"Buzz"`.
* Se for múltiplo de **ambos** (3 e 5), exiba `"FizzBuzz"`.
* Caso contrário, exiba o próprio número.

## 🚀 Diferenciais desta Versão
Diferente de uma implementação simples, este script foca na **Experiência do Usuário (UX)** no terminal:
* **Automação de Tela:** Utiliza a biblioteca `os` para limpar o terminal antes de iniciar.
* **Tempo Real:** Utiliza a biblioteca `time` para criar pausas dramáticas e uma rolagem de dados que permite a leitura humana durante a execução.



## 🛠️ Tecnologias
* **Python 3.10.8**
* Bibliotecas nativas: `os` e `time`.

## ⚙️ Como Executar
1. Certifique-se de ter o Python instalado.
2. Salve o código em um arquivo chamado `fizzbuzz.py`.
3. Execute no terminal:
   ```bash
   python fizzbuzz.py